# =============================================================================
# Maestro Agent Helm Values Override Example
# =============================================================================
# This file shows the structure of values-override.yaml
# DO NOT commit the actual values-override.yaml to Git - it contains cluster-specific values
# Generate it using: ./scripts/populate-maestro-agent-values.sh

# Management cluster identification
maestro:
  consumerName: "management-abc123"  # From management cluster Terraform

# MQTT Broker configuration
broker:
  endpoint: "a1b2c3d4e5f6g7.iot.us-east-1.amazonaws.com"  # From regional cluster Terraform

# AWS configuration
aws:
  region: "us-east-1"

# ASCP CSI Driver configuration
ascp:
  mqttCertSecretName: "management-abc123/maestro/agent-mqtt-cert"  # From regional cluster Terraform
