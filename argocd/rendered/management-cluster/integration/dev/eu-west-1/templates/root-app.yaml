apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: root
  namespace: argocd
  labels:
    argocd.argoproj.io/instance: root
    environment: '{{ .Values.environment | quote }}'
    sector: '{{ .Values.sector | quote }}'
    region: '{{ .Values.region | quote }}'
    cluster_type: '{{ .Values.cluster_type | quote }}'
  annotations:
    argocd.argoproj.io/sync-wave: '0'
spec:
  project: default
  source:
    repoURL: '{{ .Values.git_repo | quote }}'
    targetRevision: '{{ .Values.git_revision | quote }}'
    path: argocd/rendered/{{ .Values.environment }}-cluster/{{ .Values.environment }}/{{ .Values.sector }}/{{ .Values.region }}
    helm:
      valuesObject:
        environment: '{{ .Values.environment | quote }}'
        sector: '{{ .Values.sector | quote }}'
        region: '{{ .Values.region | quote }}'
        git_repo: '{{ .Values.git_repo | quote }}'
        git_revision: '{{ .Values.git_revision | quote }}'
        cluster_type: '{{ .Values.cluster_type | quote }}'
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        maxDuration: 3m
        factor: 2
    syncOptions:
    - CreateNamespace=true
